// import { usePlayerState } from '../../../src/hooks/usePlayerState';
// import { PlayerConfig, PlatformError } from 'rn-qp-nxg-player';
// import { Alert } from 'react-native';

describe('usePlayerState', function() {
    it('should return player state', async function() {
        // const playerConfig: PlayerConfig = {
        //     mediaURL: 'http://example.com',
        //     mediaType: 'HLS',
        // };
        // const onerror = (error: PlatformError) => {
        //     Alert.alert('Playback Error', `${JSON.stringify(error)}`, [{ onPress: () => { }, },]);
        // };
        // const playerState = usePlayerState({ playerConfig, onError: onerror })
        // expect(playerState).toBeDefined();
    });
});
