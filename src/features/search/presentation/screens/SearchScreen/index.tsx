import React from 'react';
import SearchResultsTemplate from 'features/search/presentation/components/templates/SearchResultsTemplate';

export default function SearchScreen({ navigation }: { navigation: any }) {
    return <SearchResultsTemplate navigation={navigation} />;
}
