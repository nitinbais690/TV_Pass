import { StyleSheet } from 'react-native';

export const VideoContentStyles = () => {
    return StyleSheet.create({
        container: {
            flex: 1,
            flexDirection: 'column',
        },
    });
};
