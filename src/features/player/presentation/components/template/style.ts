import { StyleSheet } from 'react-native';

export const platformPlayerStyles = StyleSheet.create({
    episodeListSection: {
        position: 'absolute',
        bottom: '25%',
        left: '8%',
        right: 0,
    },
});
