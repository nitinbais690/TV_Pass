import React from 'react';
import SettingsTemplate from 'features/settings/presentation/components/templates/SettingsTemplate';

export default function SettingsScreen({ navigation }: { navigation: any }) {
    return <SettingsTemplate navigation={navigation} />;
}
