import React from 'react';
import SwitchLanguageTemplate from '../../components/template/SwitchLanguageTemplate';

const SwitchLanguageScreen = ({ navigation }: { navigation: any }) => {
    return <SwitchLanguageTemplate navigation={navigation} />;
};

export default SwitchLanguageScreen;
