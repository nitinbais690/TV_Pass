import React from 'react';
import ProfileSelectionTemplate from '../components/template';

const ProfileSelectionScreen = ({ navigation }: { navigation: any }) => {
    return <ProfileSelectionTemplate navigation={navigation} />;
};

export default ProfileSelectionScreen;
