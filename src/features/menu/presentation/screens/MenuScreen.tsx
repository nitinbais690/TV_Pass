import React from 'react';
import MenuTemplate from '../components/template/MenuTemplate';

export default function MenuScreen({ navigation }: { navigation: any }) {
    return <MenuTemplate navigation={navigation} />;
}
