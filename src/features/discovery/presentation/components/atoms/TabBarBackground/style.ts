import { StyleSheet } from 'react-native';

export const tabBarBackgroundStyles = StyleSheet.create({
    positionAbsolute: {
        position: 'absolute',
        bottom: 0,
        left: 0,
        right: 0,
    },
});
