import { appDimensionValues } from 'core/styles/AppStyles';
import { StyleSheet } from 'react-native';

export const rightArrowStyle = StyleSheet.create({
    buttonCircle: {
        width: appDimensionValues.xxxxxlg,
        height: appDimensionValues.xxxxxlg,
        borderRadius: appDimensionValues.lg,
        justifyContent: 'center',
        alignItems: 'center',
    },
});
